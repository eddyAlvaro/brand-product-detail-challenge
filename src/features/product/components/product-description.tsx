export const ProductDescription = ({ children }: { children: React.ReactNode }) => {
  return (
    <section className="bg-bg rounded-[5px] p-4 text-[16px] text-brand-accent">{children}</section>
  );
};
